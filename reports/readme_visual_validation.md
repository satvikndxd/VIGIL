README visual QA findings:

The generated `docs/vigil_readme_overview.png` is a readable six-panel research snapshot showing the held-out Macro-F1 leaderboard, metric comparison, class distribution, confusion matrix, reliability curve, and measured API serving latency. Labels are legible at README scale and the chart uses the repository's blue/orange palette.

The generated `docs/vigil_ecg_waveform.png` clearly shows the real MIT-BIH record-100 waveform segment with amplitude and 360 Hz sample axes. No synthetic signal or fabricated metric is used.
